import React from 'react';
import styles from './style.module.css';

export const HomePage = (): JSX.Element => (
    <div className={styles.wrapper}>
        <p>Hi! Log in to work this your tasks</p>
    </div>
);
