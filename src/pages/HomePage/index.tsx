import React from 'react';
import styles from './style.module.css';

export const HomePage = (): JSX.Element => (
    <div className={styles.greeting}>
        Hi! Log in to work with this your tasks
    </div>
);
